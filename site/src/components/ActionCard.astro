---
import { useT } from '../i18n';
import type { Lang } from '../config/site';
interface Props { lang: Lang; text: string }
const { lang, text } = Astro.props;
const { t } = useT(lang);
---
<aside class="action">
  <h3>{t('common.what_this_means')}</h3>
  <p>{text}</p>
</aside>
